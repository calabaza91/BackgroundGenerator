{
  "name": "backgroundgenerator",
  "version": "1.0.0",
  "description": "background color gradient generator",
  "main": "script.js",
  "scripts": {
    "build":"browserify script.js > bundle.js && live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.11"
  }
}
